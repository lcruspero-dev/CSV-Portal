# CSV-Portal Docker Environment Configuration
# Copy this file to .env and update the values for your deployment
#
# Usage:
#   cp .env.docker.example .env
#   # Edit .env with your production values
#   docker-compose up -d

# ============================================
# Node Environment
# ============================================
NODE_ENV=production

# ============================================
# JWT Authentication Secrets
# IMPORTANT: Change these in production!
# Generate secure secrets with: openssl rand -base64 32
# ============================================
JWT_SECRET=your-super-secret-jwt-key-change-in-production
RESET_SECRET_KEY=your-reset-secret-key-change-in-production
UPDATE_SECRET_KEY=your-update-secret-key-change-in-production

# ============================================
# Frontend Build URLs
# These are baked into the frontend at build time
# Set to your server's public IP or domain
# ============================================
# For local development:
VITE_BASE_URL=http://localhost:5000
VITE_UPLOADFILES_URL=http://localhost:4000

# For production deployment (example):
# VITE_BASE_URL=http://your-server-ip:5000
# VITE_UPLOADFILES_URL=http://your-server-ip:4000

# For domain-based deployment (example):
# VITE_BASE_URL=https://api.yourdomain.com
# VITE_UPLOADFILES_URL=https://files.yourdomain.com

# ============================================
# Port Configuration (optional)
# Modify these in docker-compose.yml if needed
# ============================================
# API_PORT=5000          # CTS-API external port
# FILES_API_PORT=4000    # import-files-API port
# TICKETING_PORT=3000    # CSV-ticketing frontend port
# DMS_PORT=3001          # DMS-new frontend port
